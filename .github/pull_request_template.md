## 变更目标
<!-- 用一句话说明本次 PR 要解决的问题 -->

## 变更范围
<!-- 列出模块/目录；强调“仅最小必要改动” -->
- 涉及模块：
    - [ ] console-admin
    - [ ] console-ui
    - [ ] 其他（请说明）：

- 涉及文件（必填）：
    - `path/to/file1`
    - `path/to/file2`

## 需求与实现说明
<!-- 说明实现方案、关键设计点、为何这样改 -->
- 背景：
- 方案：
- 兼容性影响：

## 全局规则自检（必须全部满足）
- [ ] 已遵守 `.github/instructions/**/*.instructions.md` 与 `.github/copilot-instructions.md`
- [ ] 仅做最小且安全改动，无无关重构
- [ ] 未引入 `spring-boot-*` 依赖
- [ ] 优先复用现有 `service/mapper/util`，未重复造轮子
- [ ] 如涉及接口/字段变更，前后端已联动处理
- [ ] 如涉及日期范围筛选，已保证 `beginTime/endTime` 从 Vue → Controller/Service → Mapper 全链路一致
- [ ] 如涉及 SQL/Mapper，参数命名、条件拼接、索引使用已自查
- [ ] 已评估空值、边界值、异常路径

## 验证命令与结果（必填）
<!-- 要求：未贴命令与结果视为未完成 -->

### 1) 编译验证
```bash
# 按改动范围选择执行（至少贴实际执行过的）
cd console-ui && mvn clean compile
cd console-admin && mvn clean compile
```

结果摘要：
- [ ] 通过
- [ ] 失败（贴错误摘要）

### 2) 测试验证
```bash
# 若改动后端逻辑/接口，必须执行
cd console-admin && mvn test -DskipTests=false
```

结果摘要：
- [ ] 通过
- [ ] 无相关测试（说明原因）
- [ ] 失败（贴错误摘要）

### 3) 手工验证（如有前端交互/接口联调）
- 验证步骤：
    1.
    2.
- 实际结果：
- 截图/录屏（可选）：

## 数据库与配置影响
- [ ] 无数据库变更
- [ ] 有数据库变更（请说明脚本、执行顺序、回滚方式）

- [ ] 无配置变更
- [ ] 有配置变更（请说明环境影���）

## 风险评估与回滚方案（必填）
- 风险点：
  - 
- 回滚方式：
  - 
- 监控/观察点：
  - 

## 关联信息
- 关联 Issue/任务：
- 相关文档/讨论链接：

## Reviewer 关注点（可选）
<!-- 告诉 Reviewer 重点看哪些文件/逻辑 -->
- 